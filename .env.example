# Virtual Matter Bridge Configuration
# Copy this file to .env and adjust values as needed

# Network interface for Matter/mDNS (auto-detected if not set)
# MATTER_INTERFACE=enp14s0

# RTSP Camera Configuration
RTSP_URL=rtsp://username:password@192.168.1.100:554/h264Preview_01_main
RTSP_USERNAME=username
RTSP_PASSWORD=password

# Matter Device Configuration
DEVICE_NAME=Virtual Doorbell
MATTER_DISCRIMINATOR=3840
MATTER_PASSCODE=20202021

# MQTT Broker Configuration
# MQTT_BROKER_HOST=192.168.1.50
# MQTT_BROKER_PORT=1883
# MQTT_CLIENT_ID=virtual-matter-bridge
# MQTT_USERNAME=mqtt_user
# MQTT_PASSWORD=mqtt_pass

# Logging level (error, warn, info, debug, trace)
# Use debug to see UDP packet flow, trace for full packet dumps
RUST_LOG=info

# Development Tools
#
# MATTER_SERVER_URL: WebSocket URL to python-matter-server for automated commissioning
# - Home Assistant OS/Supervised: ws://homeassistant.local:5580/ws
# - Docker standalone: ws://<docker-host-ip>:5580/ws
# MATTER_SERVER_URL=ws://localhost:5580/ws
#
# DEV_AUTO_RESET: Force persistence reset on every startup (always re-commission)
# DEV_AUTO_RESET=1
